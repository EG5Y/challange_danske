{
  "name": "fe-todo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/fe-todo",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/fe-todo",
        "postcssConfig": "apps/fe-todo/postcss.config.js"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/fe-todo"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "fe-todo:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "fe-todo:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "fe-todo:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "fe-todo:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/fe-todo/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/fe-todo/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
